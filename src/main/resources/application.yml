spring:
  application:
    name: verifier-oid4vp
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      timeout: 30000
  docker:
    compose:
      enabled: false


server:
  max-http-request-header-size: 40KB