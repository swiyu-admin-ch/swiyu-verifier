server:
  port: 8080
spring:
  application:
    name: verifier-agent-management
  cloud:
    kubernetes:
      config:
        enabled: false
      secrets:
        enabled: true
        paths: /data/secrets
